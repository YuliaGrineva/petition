<p>These are the bravest people who have signed this petition:</p>
<div id="sign">
{{#signers}}

    {{#if url}}
        <p><a href={{url}}> {{firstname}} {{lastname}}</a>

            {{age}}
            <a href="/signers/{{city}}">{{city}}</a></p>
    {{else}}
        <p>{{firstname}}
            {{lastname}}

            {{age}}
            <a href="/signers/{{city}}">{{city}}</a></p>
    {{/if}}
{{/signers}}
</div>
<div id="editButton">
<a href="/profile/editing"><button>EDIT PROFILE</button></a>
<a href="/"><button>LOG OUT</button></a>
</div>
{{! <p id="allSigners"></p> }}

{{! /signers/:city/ }}